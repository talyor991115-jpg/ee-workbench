# 龚逸凡 — AI 产品经理

**手机** 18905102407 ｜ **邮箱** 1345664655@qq.com ｜ **期望薪资** 15-20K

---

## 我不是传统产品经理

市面上大多数 AI 产品经理的工作方式：画原型、写 PRD、开会对齐、等开发排期、催进度。他们和 AI 的关系是「听说这个技术很火，能不能用在我们的产品里」。

我的工作方式不一样。

五个上线产品不是画完原型丢给开发团队做的，是我一个人用 AI 驱动，从需求分析到编码到上线的完整闭环。我用的不是 ChatGPT 聊天框，是搭了一套 AI Agent 工程系统——167 个专业 Agent 各司其职，我来编排调度，让一群 AI 像一个小型开发团队一样协同工作。

这意味着什么？意味着我能在几分钟内用 AI 搭一个可交互的产品草稿出来验证想法。意味着和工程师讨论方案时，我能聊到代码架构和接口设计的粒度——因为我自己就写过。意味着我知道 AI 在什么场景下确实能降本增效，在什么场景下会翻车、需要人兜底。这些判断不是从行业报告里读来的，是踩了无数次坑之后沉淀下来的。

---

## AI 中台体系 — 从个人痛点变成系统工程

这件事的起点是一个很朴素的观察：我用 AI 写代码的时候，经常会遇到三个问题。第一，AI 在长对话里会忘掉之前约定的规范（API 格式、命名规则、项目结构），反复提醒很烦。第二，AI 产出的代码质量不稳定，有时好有时坏，没有质量检验机制。第三，我每次都要手把手告诉它项目背景，上下文成本很高。

为了解决这几个问题，我搭了一套系统：

**Agent 编排层（167 个 Agent，15 个业务域）**

每个 Agent 是某个领域的专精角色——架构师只负责做方案设计、前端工程师只写页面代码、代码审查员只检查 bug、测试工程师只写测试用例。一个复杂需求进来，架构 Agent 先拆成子任务 → 分发到对应的执行 Agent → 审查 Agent 验证 → 汇总交付。这套流程把一个模糊的需求变成了一条可预期的生产线。

做过最复杂的案例是调音师平台的订单模块。涉及用户下单、微信支付回调、库存扣减、订单状态流转（七个状态）、调音师端同步、管理后台审核。传统开发需要产品画完逻辑图再和前后端分别对齐。我的做法是直接把业务场景描述给架构 Agent → 拆成 12 个子任务 → 5 个不同领域的 Agent 并行执行 → 代码审查 Agent 逐一检查 → 联调修正 → 交付。从需求开始到可运行的订单模块，不到三天。

**技能模块库（55 个 Skill）**

把高频的开发动作封装成可复用的 Skill。比如「页面生成」Skill 内置了微信小程序的页面结构规范、命名规则、API 调用格式，调用它生成的代码不需要我再手动调整。「数据分析」Skill 能直接解析 Excel/CSV、做统计聚合、生成 SQL。每个 Skill 是独立模块，改一个不影响其他，新增一个不影响旧的。和传统开发里的公共组件库是同一个道理，只不过封装的是 AI 的行为模式而不是代码。

**自动化流水线**

每次启动项目时，系统自动检测环境（Node.js 版本、依赖是否安装、sharp 工具链是否就绪），加载对应项目的规则文件。每次文件修改后自动记录操作日志，方便回头排查。对话上下文太长需要压缩时，自动保存快照防止关键信息丢失。这些事很小，但加起来省掉了我大量的心智负担——不用每次跟 AI 说「我们这个项目的 API 返回格式是 { code, data, message }」，系统自动给它注入了。

**结果评估闭环**

AI 写出来的代码不是直接用的。审查 Agent 会按正确性、安全性、可维护性三个维度逐行检查，给出「必须修改」「建议优化」「通过」三个等级的结论。修改完再跑测试验收。这套闭环保证了我一个人能持续产出达到上线质量的产品——不是靠运气，是靠机制。

---

## 产品实战项目

### 健联健身平台 ｜ 微信小程序 ｜ 产品设计 + 全栈交付

面向健身行业的数字化平台，连接消费者、健身房、教练三方。

**为什么做这个**

健身房行业有个很奇怪的现象：商家花几十万开个店，获客还是靠发传单。用户想找个靠谱健身房，要么是被销售忽悠、要么是靠朋友推荐。教练预约全靠微信聊天，能不能约上、几点上课、临时改时间怎么办，全凭口头约定。我把这三边的痛点串在一起，设计了一个平台来统一解决。

**产品架构**

三端架构：用户端小程序（C 端）+ 健身房商家后台（B 端）+ 平台运营后台。

用户端 5 大底部导航，20+ 页面：
- 首页：附近健身房推荐、会员状态、热门活动、今日课程、社区动态
- 健身房发现页：三种浏览模式（列表 / 地图），按距离、价格、评分、设施四个维度筛选
- 预约系统：私教（选教练 → 看时间 → 选时段 → 支付/扣次数）、团课、公共课程、洗浴，四个预约入口统一管理
- 健友圈：动态浏览/发布、点赞评论、健身打卡、话题互动——社区是留存的核心
- 个人中心：会员卡管理、订单/积分/优惠券、邀请好友裂变、收藏

商家后台覆盖三种角色权限：老板全权限、店长运营管理、教练课程管理。功能包括会员管理、排班、课程发布、活动管理、经营数据看板。

**关键产品决策**

1. 用户身份三状态模型。这是整个产品最核心的设计决策。用户不是一进来就是会员——大部分是刷着玩的游客。如果一打开小程序就弹窗「请购买会员」，人早跑了。所以设计了三种状态自动切换：游客（浏览模式，能看到附近健身房和体验券但无法预约）→ 单店会员（专属主页 + 完整预约功能）→ 多店会员（增加多健身房切换入口，类似支付宝切换账号）。这个设计的灵感来自我在运营公会时对不同层级管理权限的思考。

2. 体验券转化漏斗。传统的健身房销售套路是「免费体验 → 跟进逼单」，信任成本很高。我设计的产品路径是：用户在社区看别人打卡 → 领体验券去试课 → 试用满意后买会员 → 预约课程 → 课后在社区分享 → 邀请好友。体验不再是销售手段，而是融入产品体验的自然一环。这和美团的「逛→看评价→团购→到店消费」是同一套转化逻辑。

3. Design System 的坚持。整个产品严格遵循了一套 Apple 风格的设计规范——品牌色深空蓝 #1D3557、70% 灰白背景 + 20% 白色卡片 + 10% 品牌色的色彩比例、8px 间距体系、卡片圆角 24px、按钮圆角 999px。市面上大多数健身 App 都是大红大绿、荧光橙配黑底，一看就像健身房发的那种廉价传单。我要做的质感是做给苹果用户看的，不是做给健身房地推用的。这个决策背后有个产品判断：现在健身的年轻人大部分用的是 iPhone，他们每天刷的是小红书和 Instagram，对美感的阈值很高。如果你的产品长得像十年前的样子，他们根本不会下载，更不会在里面花钱。

### 调音师 C2C 平台 ｜ 微信小程序 ｜ 产品设计 + 全栈交付

一个调音师服务的撮合交易平台，含用户端、调音师端、管理后台三大系统。

**需求洞察**

我在公会运营时发现，主播找调音师这件事特别原始。全凭熟人推荐、微信转账、口头约定。调音效果好不好没有客观标准，做完了不满意扯皮没人管。调音师那边也烦——靠朋友介绍客源不稳定，价格被压得很低，不知道该收多少合适。这是一个典型的「平台机会」：两边都有需求，但中间没有信任机制。

**产品设计**

交易链路：用户浏览调音师详情页（头像、简介、作品展示、用户评价、价格表）→ 下单选择服务类型和规格 → 微信支付 → 调音师端收到订单通知 → 接单 → 服务完成后提交交付物 → 用户验收确认 → 双方互评 → 调音师在钱包提现。

订单状态机是核心设计。七个状态：待接单 → 已接单 → 服务中 → 待验收 → 已完成 → 已评价 → 已提现。每个状态切换有明确的触发条件和责任方。比如「服务中」只有调音师可以点击「完成服务」推到「待验收」；「待验收」只有用户可以点击「确认验收」推到「已完成」。状态机保证了双方的操作边界清晰，不会出现「调音师说自己做完了、用户说没做完」的扯皮。

支付设计：用户付的钱不是直接给调音师的，是先到平台担保。用户验收确认后，调音师才能申请提现。这解决了信任问题——用户不怕付了钱对方消失，调音师不怕做了事收不到钱。

AI 智能客服：接入 DeepSeek API，把常见问题（调音师怎么入驻、订单怎么取消、提现多久到账、退款规则等）做成知识库，用户在小程序内直接问，不需要加微信找人。匹配推荐也是通过 AI 做——根据用户描述的需求（直播平台、预算、风格偏好）自动推荐合适的调音师。

**管理后台**

五个功能模块：用户管理（注册信息、实名认证审核、会员状态）→ 订单管理（所有订单列表、状态筛选、异常订单处理）→ 数据看板（日活、订单量、GMV、转化率）→ 提现审批（调音师提现申请审核、批量打款）→ 内容管理（首页 Banner、推荐位、公告、规则说明）。

**做的过程中的一个关键产品洞察**

AI 写代码有个很大的问题：它不会反问。你说「做一个下单页面」，它立刻开始写，不会问你「支付方式有什么？要不要优惠券？库存怎么处理？下单后是直接支付还是先确认？」这些在传统开发流程里，后端、前端、产品三方在评审时会被反复追问的问题，AI 不会问。它默认你的需求是完整的。

这就导致了一个很危险的情况：非技术用户用 AI 做产品，以为自己说清楚了，AI 也写出来了，但实际上里面全是坑。我在做这个项目的时候就意识到，AI 编程对普通用户最大的障碍不是「代码怎么写」，而是「我该怎么描述需求才能让 AI 产出靠谱的东西」。这其实是产品经理的核心能力——需求拆解——只不过拆解的对象从工程师变成了 AI。

### 点歌互动小程序 ｜ 微信小程序 ｜ 产品设计 + 全栈交付

面向直播间场景的观众实时点歌系统。

这个产品的需求来源很直接：直播间里弹幕刷屏太快，主播根本没时间看谁点了什么歌。经常是观众花了钱点歌，等了半小时没轮到自己，体验很差。把场景拆开来看其实很简单：观众想被听到、主播想有序管理、需要一套规则。

产品设计三端分离：观众端（搜索歌曲 → 选择版本 → 提交点歌 → 加入排队列表 → 看到自己排第几位）→ 主播端（点歌队列管理面板 → 歌曲状态切换：等待中 / 正在播放 / 已跳过 → 一键标记已播）→ 管理后台（歌单库管理、点歌记录、直播场次统计）。实时性是核心要求——排队列表必须实时刷新，不能观众提交了 30 秒才看到变化。

技术实现用了微信云开发 CloudBase 的数据库实时监听能力，前端订阅数据变化自动更新视图，不需要轮询。整套逻辑我自己写的云函数，不到三天跑通。

### AI 桌宠 ｜ Electron 桌面应用 ｜ 产品设计 + 全栈交付

把虚拟主播的 Live2D 模型变成桌面 AI 伙伴。

**产品定位**

这是一个 B2B2C 的产品思路。第一阶段先自己用：把公会旗下主播的皮套做成桌宠，送给粉丝当福利（购买会员送专属桌宠、打榜前三名送定制版）。验证可行性后第二阶段做批量接单生意：帮主播做定制桌宠——壳做一次，每单只换三样东西（Live2D 模型文件、角色人设 Prompt、声音样本），每单利润在 500-2000 元不等。第三阶段可以做 SaaS：主播自己在平台上传模型、配置人设、生成专属桌宠，按月收费。

**交互设计**

桌面交互层：透明无边框悬浮窗（不遮挡桌面其他内容）、鼠标穿透（能点击到桌宠下方的东西，不会挡操作）、拖拽移动（拖头拖身体都能动）、系统托盘（不用时收起来不占屏幕）。Live2D 渲染层：视线跟随鼠标自然追踪、点击身体不同位置触发不同的互动反馈（摸头抖一下、点肚子会害羞、点脸生气）、表情控制面板手动切换 14-15 个表情。

AI 对话层：按角色人设构建 System Prompt，聊天的回复会自动提取情绪标签 → 映射到 Live2D 参数 → 触发对应表情和身体动作。比如聊到开心的事，角色自动露出笑脸；聊到难过的话题，表情切到委屈。这个情绪联动让桌面宠物从「会动的图片」变成了「有情感反应的伙伴」。

**技术选型决策**

核心问题是选什么技术来做 Live2D 桌面渲染。调研了三条路：Unity 方案（太重，打包出来几百 MB，不适合做轻量桌宠，而且我没 C# 基础）、Vtuber Desktop 等开源工具（不是 Live2D 专用、功能不对口）、pixi-live2d-display 方案（纯 JavaScript，Electron 直接加载，打包体积小，社区活跃，学习成本可控）。最终选了第三条路，事实证明决策正确——不需要学新语言，不需要搞重型引擎，整套东西一个人一周跑通了核心 Demo。

**两个技术难点**

8192 分辨率贴图（凤凰角色）在 WebGL 里启动加载 20+ 秒。用户打开桌宠让它卡 20 秒是不可接受的。定位到问题是纹理上传阶段的 GPU 传输瓶颈——贴图太大，上传就占掉了大部分时间。解决方案是做纹理分级加载：启动时先加载 2048 缩略版保持交互可用（3 秒左右），后台异步上传 8192 完整版，上传完成后无缝替换。这个逻辑还没写进正式版但技术路径已验证。

Windows 不同缩放比例下透明窗口坐标偏移。用户电脑设置了 125% 或 150% 缩放后，点击位置和实际渲染位置对不上。原因是有几行代码直接用了屏幕像素坐标，没考虑 DPI 缩放。修复方式是拿到显示器实际 DPI 做坐标换算，十来行代码解决。

**产品化思考**

Config 驱动的一键换宠是整个产品最核心的设计决策。config.json 里有一个 active 字段指向当前激活的角色，pets 数组里每只宠物独立配置——模型文件路径、角色人设 Prompt、情绪标签映射表、面板表情列表、点击反应细节。壳和内容完全分离。这意味着新增一只宠物不需要改代码，改一个 JSON 文件即可。这个架构也是批量接单的基础——每接一个定制单，我只需要从主播那里拿到模型文件 + 写好角色 Prompt + 配好音色，config 里加一条记录，就能交付。壳只做一次，后续全是流水线作业。

### 星枢纪官网 ｜ 品牌官网 ｜ 产品设计 + 前端交付

原创玄幻世界观 IP 的多页面品牌展示官网。

**内容架构**

20+ 子页面按五个内容板块组织：世界观总览（时代纪元设定、归墟核心概念、虚空档案、历史编年）→ 四方文明（白虎/青龙/玄武/朱雀四大文明独立页面，每个有独立的视觉风格）→ 四方角色（核心角色的独立主页 + 角色关联关系图）→ 剧情系统（毒系五念：贪嗔痴疑傲的设定页面）→ 星宿体系（二十八星宿的权威展示）。

这样组织不是凭感觉，是因为这个 IP 的内容体量大、设定体系复杂，如果做成传统小说官网那种「一个页面划到底全塞进去」，读者根本看不完，信息密度太高。按内容板块拆分后，每个板块有自己的视觉节奏，读者可以选择性探索，更像一个「可互动的世界观浏览器」而不是「宣传单页」。

**交互设计**

做了一套 CSS 动效——毒性轮盘页面里，角色点击之后周围的毒系元素有一个旋转扩散的动效，模拟毒素在身体里蔓延的感觉。这个东西如果用 JS 写会很长，用了 CSS animation + transform，几十行代码就实现了，而且不卡。

**产品价值**

这个网站的核心作用是把文字描述的世界观可视化。星枢纪是一个构建了四年多的 IP，有几十万字的设定文档，但读者很难从文字里感受到「这是一个怎样的世界」。网站通过图片、动效、页面结构的设计，把一个抽象的世界变成可以被感知的空间。粉丝运营的一个关键节点是「仪式感」——你有一个专属的页面、有精美的视觉设计、有体系化的内容呈现，读者才会觉得这个世界是真的存在的。

---

## AI 工具能力

不是我了解这些工具，是这些工具嵌在我日常工作的每个环节里：

| 维度 | 具体实践 |
|------|---------|
| Claude Code | 主力开发环境。所有产品项目通过它驱动交付。不是偶尔试试，是像 IDE 一样每天开着的状态。用它的过程中积累了大量关于「怎么跟 AI 描述需求」「怎么防止 AI 跑偏」「怎么让 AI 产出可维护代码」的第一手认知 |
| DeepSeek API | 实际集成到三个产品中：调音师 AI 智能客服（知识库问答 + 调音师匹配推荐）、桌宠 AI 对话（角色扮演 + 情绪识别 + 表情联动）、健联平台的用户咨询。不是接了个 Demo，是生产环境在跑 |
| Prompt 工程 | 分层规则体系——项目级规则（CLAUDE.md）设定宏观规范，领域级规则（前端/后端/API/组件/文档五个文件）约束具体实现细节，项目专项规则（彗星号开发铁律等）处理特殊情况。这些规则按当前工作上下文自动注入，保证 AI 产出的一致性和可预期性。这是把 Prompt 从一次性调参变成了可复用的工程资产 |
| Agent 编排 | 167 个 Agent 的调度系统。不是概念，是每天都在运转的生产设施。每个 Agent 有独立的角色定义、能力范围、输出规范，通过统一的编排层协同工作 |

---

## 业务理解力

**众诚保险 ｜ 人伤理赔 ｜ 2022 — 2025**

三年在保险公司一线做车险人伤理赔。处理了几百个真实案件，每一个案子的流程是：客户报案 → 现场查勘 → 伤者就诊 → 医疗费审核 → 伤残鉴定 → 责任认定 → 调解协商 → 赔偿方案制定 → 结案归档。听起来很流程化，但每一个环节都有大量例外和细节判断。

在这个过程中我发现了两个系统缺陷，主动提了优化方案并被公司采纳：

第一个是图片材料的管理。每个案子都有几十张照片——事故现场、车辆受损、伤者病历、发票、身份证明。不同阶段的照片容易混在一起，责任归属不清楚。我提的方案是「上传时自动添加水印」，标注上传时间、操作人员、案件编号，出问题能追溯。这个需求看起来很小，但对合规和纠纷处理的影响很大。

第二个是客户信息重复录入。同一个客户在不同页面频繁出现相同字段（姓名、身份证号、联系方式），理赔员要手动填好几遍，容易出错。我提的「一键调取历史信息」方案，减少了一半以上的重复录入操作。当时还没接触产品经理这个岗位，但回头看——发现痛点、分析根因、提出方案、推动落地，这就是产品思维。

**济南零轨传媒 ｜ 运营负责人 ｜ 2025 — 至今**

从零开始搭建公会的运营体系和业务支撑系统。这个过程和在一个创业公司做产品负责人的体验几乎完全相同——需要同时处理业务增长（拉主播、做内容、跑营收）、产品搭建（从零做了几个业务工具）、资源分配（有限的资源怎么投到最高产出的地方）、团队协作（和主播、行政、财务、技术多方沟通）。

公会运营的经历让我对内容创作者生态有了一手体感。主播的创作流程（选题→编曲→录制→后期→发布→数据分析→调整方向）、收入模式（打赏+广告+商单+知识付费+私域引流）、和公会的关系（流量扶持 vs 分成比例 vs 日常管理）——这些都是做创作者工具类产品的用户画像。

---

## 教育

本科（法学）

---

## 关于我

市面上 AI 产品经理候选人大概分两类。一类是从传统产品经理转过来的，懂产品方法论、懂 PRD、懂向上汇报，但 AI 只是他们工具箱里的一个新鲜名词——他们用得最多的还是 ChatGPT 聊天框，对大模型能力边界的判断来自阅读而非实践。另一类是技术出身转产品，懂模型、懂架构、能聊向量数据库和 RAG，但不太理解一线业务用户到底在想什么。

我的路径不太一样。保险三年让我理解了一个传统行业里真实的工作流——系统长什么样、人怎么用它、痛点在哪。用 AI 独立做了五个产品让我知道了 AI 现在能解决什么、不能解决什么，不是从书里看的，是一个一个坑踩过来的。搭 AI 中台的过程中锻炼了系统化思维——事情不是靠一次一次调 prompt 做的，是靠工程机制保证产出的。

如果你需要一个会画原型、会写 PRD、会组织需求评审的产品经理，市面上有很多。如果你需要一个理解 AI 能力边界、能和工程师聊到代码层面、还能自己上手快速验证想法的产品经理，我们可以聊聊。
