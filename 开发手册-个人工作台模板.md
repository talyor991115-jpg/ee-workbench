# 个人工作台模板 — 开发手册

## 一、项目概述

单 HTML 文件个人工作台，响应式设计，电脑端侧边栏 + 手机端底部导航。部署到 GitHub Pages 零成本运行。

**技术栈**：HTML + CSS + Vanilla JS，零框架依赖，零构建工具。

---

## 二、标准开发路线

### 第1步：确认用户画像（30分钟）
- 职业、日常事务、痛点
- 使用的设备（电脑/手机比例）
- 是否需要 AI 功能
- 数据隐私要求

### 第2步：确认模块清单（30分钟）
必须覆盖六类能力：
1. **今日** — 第一优先级，聚合所有待处理事项
2. **个人工作模块** — 按用户真实工作拆 2-4 个
3. **收集箱/空间** — 快速记录、笔记管理
4. **搜索** — 跨模块全文检索
5. **AI 帮手** — 对话、分析、生成
6. **设置与数据** — 使用说明、导入导出、更新日志

### 第3步：搭建设计系统（1小时）
CSS 变量统一管理：
```
颜色（背景、卡片、文字、强调、警示）
字体（中文苹方、英文 SF Pro）
间距（8px 体系）
圆角（卡片 24px、小元素 12px、按钮 999px）
阴影（极淡分层）
动画（200-400ms ease-out）
```

### 第4步：搭建 HTML 骨架（1小时）
- 登录页（可选）
- 侧边栏（240px，用户头像 + 导航 + 底部设置）
- 顶部工具栏（日期 + 搜索框，毛玻璃）
- 主内容区（max-width 去掉，自适应窗口）
- 手机底部导航（4 等分，最多 5 个）
- 悬浮 AI 按钮（右下角圆形）
- 更多菜单（手机端底部弹出面板）

### 第5步：实现数据层（30分钟）
```javascript
const D = {
  // 所有数据存这里
  settings: { aiKey: '', ... },
  // 各模块数据
};
// localStorage 读写
// 导出导入 JSON
```

### 第6步：逐模块开发（每个 30-60 分钟）
按优先级顺序：
1. 今日（聚合页）
2. 核心工作模块
3. 公共能力（搜索、AI、设置）
4. 辅助模块（健康、学习等）

### 第7步：接入实时数据（30分钟）
- 股市：腾讯 qt.gtimg.cn（CORS 友好）
- 金价：api.gold-api.com（CORS 友好）
- AI 功能：DeepSeek API（OpenAI 兼容格式）

### 第8步：部署上线（15分钟）
1. Git init → commit
2. GitHub 建仓库 → 生成 SSH key → 推送
3. Settings → Pages → 选 main 分支 → 获得网址
4. index.html 作为入口文件

---

## 三、核心踩坑记录

### 坑1：CORS 跨域（耗时最长）
**现象**：所有前端 fetch API 请求被浏览器拦截，显示「离线」。
**原因**：金融数据 API（东方财富、新浪、腾讯）绝大多数不设 CORS 头。
**解决**：
- 股市：腾讯 `qt.gtimg.cn` 实测返回 `Access-Control-Allow-Origin: *`
- 金价：`api.gold-api.com` 实测返回 `Access-Control-Allow-Origin: *`
- 绝对不要用东方财富 `push2.eastmoney.com`（无 CORS 头）
- 绝对不要依赖 DeepSeek 返回实时数据（AI 不联网）
- 不要用公共 CORS 代理（ corsproxy.io 等全挂了）

### 坑2：DeepSeek API 模型名
**现象**：所有 AI 功能静默失败。
**原因**：模型名 `deepseek-chat` 已废弃，新版是 `deepseek-v4-pro` 和 `deepseek-v4-flash`。
**解决**：用 `deepseek-v4-flash`（无 thinking 块，直接返回文本）。
**教训**：API 集成前先用 curl 测试一遍。

### 坑3：v4-pro 的 thinking 块
**现象**：AI 返回空内容。
**原因**：`deepseek-v4-pro` 是推理模型，回复中有 `type: "thinking"` 的 content block，`content[0].text` 取到空值。
**解决**：代码里先过滤 `content.find(c => c.type === 'text')` 再取 `.text`。或者直接用 v4-flash。

### 坑4：DeepSeek JSON 返回格式不稳定
**现象**：AI 生成的题目、三餐方案等 JSON 解析失败。
**原因**：DeepSeek 经常在 JSON 外包 markdown 代码块、用中文标点、加额外文字。
**解决**：写 `tryParseJSON()` 和 `parseJSONObj()` 函数，自动剥离 markdown 标记、替换中文标点、正则提取 JSON 部分。失败时自动重试一次。

### 坑5：onclick 属性在复杂页面中失效
**现象**：按钮点击完全没反应，连 toast 都不弹。
**原因**：CSP（Content Security Policy）可能拦截 inline event handler。加上页面 innerHTML 频繁重写导致事件绑定丢失。
**解决**：
- 在 `<head>` 加 CSP meta 标签放行 `unsafe-inline` 和 `unsafe-eval`
- 关键按钮改用 `document.createElement` + 直接 `.onclick = function(){}` 绑定
- 用事件委托（document.addEventListener）做兜底
- 避免用箭头函数和模板字符串做 onclick（它们在某些环境下不稳定）
- 弹窗按钮直接用 DOM 引用绑定，不用 getElementById 查找

### 坑6：弹窗重复出现
**现象**：点击一次出现两个弹窗。
**原因**：事件委托和 onclick 属性同时触发，或页面重渲染导致绑定两次。
**解决**：
- 在弹窗创建函数开头检查 `if(document.getElementById('xxx'))return` 防重复
- 弹窗按钮用 `createElement` 直接绑定，不用 HTML onclick 属性

### 坑7：toISOString() 时区偏移
**现象**：日期显示差一天。
**原因**：`new Date()` 在东八区午夜时，`toISOString()` 按 UTC 返回前一天。
**解决**：用自写 `fmtLocalDate()` 函数：`d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')`

### 坑8：const 重新赋值导致页面全白
**现象**：页面完全空白。
**原因**：`const dow = i+1; if(dow===7) dow=0;` — const 声明后不可重新赋值，JS 直接抛异常。
**解决**：用 `let` 或直接 `const dayNum = i===6 ? 0 : i+1`。

### 坑9：GitHub Pages 部署连通性
**现象**：HTTPS 443 端口 push 失败。
**原因**：国内网络 443 端口被墙。
**解决**：用 SSH（22 端口）推送：`git remote set-url origin git@github.com:xxx/xxx.git`

### 坑10：手机端按钮触摸区域太小
**现象**：手机端按钮难点中。
**原因**：未声明最小触摸区域 44x44px。
**解决**：按钮 `padding: 10px 24px; font-size: 14px`，导航项高度 64px。

### 坑11：手机端底部导航入口挤在左边
**现象**：4 个入口不全宽。
**原因**：未用 `flex: 1` 均分。
**解决**：`.mob-nav-item { flex: 1 }`，4 个入口四等分。

### 坑12：innerHTML 频繁重写导致输入框失焦
**现象**：搜索框输入一个字符就失焦。
**原因**：`oninput="render('space')"` 触发全页重渲染，输入框被替换。
**解决**：搜索词存到变量 `spaceSearchTerm`，渲染时复原 value，且不要用 `inp.value = ''; inp.value = v` 这种可能触发二次 oninput 的操作。

---

## 四、标准接单流程

### 需求收集模板
```
1. 做什么工作？日常主要做哪几件事？
2. 最想解决的 3 个问题？
3. 希望 AI 主要帮什么？
4. 电脑和手机都常用吗？
5. 有特殊的 UI 偏好吗？（发参考图）
```

### 交付清单
- [ ] 电脑端和手机端都能打开
- [ ] 所有按钮可点击，弹窗可取消可保存
- [ ] 新建、编辑、删除功能正常
- [ ] 搜索能跨模块检索
- [ ] 无 emoji，全部 SVG 图标
- [ ] 无 console.log 残留
- [ ] 无硬编码假数据，空状态显示引导文案
- [ ] 登录功能正常（如有）
- [ ] 手机端无横向溢出
- [ ] 手机端底部导航均分全宽
- [ ] 部署到 GitHub Pages 并验证实时数据
- [ ] 设置页有完整使用说明和更新日志

### 给客户的部署说明
```
1. 打开网址，账号 xx，密码 xx
2. 设置页填入 DeepSeek API Key（platform.deepseek.com 免费注册）
3. 黄金和股市自动实时刷新，无需配置
4. 数据存在浏览器，建议每周导出备份
5. 换设备需手动导入 JSON 备份文件
```

---

## 五、文件结构

```
项目文件夹/
├── ee个人工作台.html    # 主文件
├── index.html           # GH Pages 入口（同主文件）
├── bg-desktop.jpg       # 桌面端背景
├── bg-mobile.jpg        # 手机端背景
└── 开发手册-个人工作台模板.md
```

---

## 六、常用命令

```bash
# 本地语法检查
node --check ee个人工作台.html

# 部署
git add . && git commit -m "xxx" && git push

# GitHub Pages 验证
curl -s -o /dev/null -w "%{http_code}" "https://用户名.github.io/仓库名/"

# 测试 API CORS 支持
curl -s -I "API地址" -H "Origin: https://你的域名" | grep access-control
```
